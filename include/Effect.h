#ifndef EFFECT_H
#define EFFECT_H


//class effect
// Fonction Apply(ressources a modifier, valeur a utiliser, operateur a utiliser); applique l'effet
// nom, enum tag

class Effect
{
    public:
        Effect();
        virtual ~Effect();

    protected:

    private:
};

#endif // EFFECT_H
